# Copy this to .env and fill in your key (do NOT commit the real .env file)
# Optional — only needed for the live AI Storage Story narrative.
# Without it, AegisStore falls back to a template narrative automatically.
ANTHROPIC_API_KEY=
